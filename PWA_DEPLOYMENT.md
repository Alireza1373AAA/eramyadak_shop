# راهنمای استقرار PWA فروشگاه ارام یدک

## 📦 فایل‌های بیلد

فایل PWA در مسیر `build/web` ایجاد شده است و شامل موارد زیر است:

- `index.html` - صفحه اصلی
- `main.dart.js` - کد اپلیکیشن (2.8 MB)
- `manifest.json` - تنظیمات PWA
- `flutter_service_worker.js` - Service Worker برای کار آفلاین
- `assets/` - دارایی‌های اپلیکیشن
- `icons/` - آیکون‌های PWA

## 🚀 استقرار

### روش ۱: استقرار مستقیم
فایل‌های داخل `build/web` را به هاست یا سرور خود آپلود کنید.

### روش ۲: استفاده از آرشیو
فایل فشرده شده: `eramyadak_shop_pwa.tar.gz` (7.5 MB)

```bash
# استخراج فایل‌ها
tar -xzf eramyadak_shop_pwa.tar.gz
```

## 🌐 سرویس‌های هاست پیشنهادی

### Firebase Hosting (رایگان)
```bash
npm install -g firebase-tools
firebase login
firebase init hosting
# انتخاب build/web به عنوان public directory
firebase deploy
```

### Netlify (رایگان)
1. وارد [netlify.com](https://netlify.com) شوید
2. پوشه `build/web` را Drag & Drop کنید
3. دامنه سفارشی اضافه کنید (اختیاری)

### Vercel (رایگان)
```bash
npm i -g vercel
cd build/web
vercel --prod
```

### GitHub Pages (رایگان)
```bash
cd build/web
git init
git add .
git commit -m "PWA deployment"
git branch -M gh-pages
git remote add origin YOUR_REPO_URL
git push -u origin gh-pages
```

## ⚙️ تنظیمات PWA

فایل `manifest.json` شامل:
- **نام:** فروشگاه ارام یدک
- **رنگ تم:** #FF7300 (نارنجی)
- **زبان:** فارسی (RTL)
- **حالت نمایش:** Standalone (مانند اپ نیتیو)

## 📱 نصب روی موبایل

کاربران می‌توانند از مرورگر گزینه **"Add to Home Screen"** را انتخاب کنند.

## ✅ بهینه‌سازی‌ها

- ✅ Tree-shaking فونت‌ها (کاهش 99% حجم آیکون‌ها)
- ✅ Service Worker برای کار آفلاین
- ✅ رندرر CanvasKit برای عملکرد بهتر
- ✅ کامپایل Release Mode

## 🔒 نکات امنیتی

در صورت استفاده از HTTPS:
- PWA کامل فعال می‌شود
- دسترسی به قابلیت‌های پیشرفته مرورگر
- نصب روی صفحه اصلی دستگاه

## 📊 حجم فایل‌ها

- کل پروژه: 24 MB
- فایل فشرده: 7.5 MB
- فایل اصلی JS: 2.8 MB

---

✨ بیلد شده با Flutter 3.24.5
📅 تاریخ: 28 نوامبر 2025
